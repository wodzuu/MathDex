/// <reference types="vite/client" />
/// <reference types="vite-plugin-pwa/react" />

/** Build timestamp (ISO 8601), injected at build time by Vite (`define`). */
declare const __BUILD_TIME__: string;
